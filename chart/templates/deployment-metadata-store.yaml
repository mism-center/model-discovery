{{- if .Values.metadataStore.enabled }}
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: metadata-store
  labels:
    {{- include "model-discovery.labels" . | nindent 4 }}
    app.kubernetes.io/component: metadata-store
spec:
  serviceName: metadata-store
  replicas: 1
  selector:
    matchLabels:
      {{- include "model-discovery.selectorLabels" (dict "component" "metadata-store" "context" $) | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "model-discovery.selectorLabels" (dict "component" "metadata-store" "context" $) | nindent 8 }}
    spec:
      {{- include "model-discovery.imagePullSecrets" . | nindent 6 }}
      containers:
        - name: postgres
          image: "{{ .Values.global.imageRegistry }}{{ .Values.metadataStore.image.repository }}:{{ .Values.metadataStore.image.tag }}"
          imagePullPolicy: {{ .Values.metadataStore.image.pullPolicy }}
          ports:
            - name: postgres
              containerPort: {{ .Values.metadataStore.service.port }}
              protocol: TCP
          env:
            - name: POSTGRES_DB
              value: {{ .Values.metadataStore.env.POSTGRES_DB | quote }}
            - name: POSTGRES_USER
              value: {{ .Values.metadataStore.env.POSTGRES_USER | quote }}
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: model-discovery-secrets
                  key: databasePassword
            # PVC root contains lost+found on ext4; point PGDATA at a configurable subdir so initdb sees an empty target.
            - name: PGDATA
              value: {{ .Values.metadataStore.pgData | quote }}
          livenessProbe:
            exec:
              command:
                - pg_isready
                - -U
                - {{ .Values.metadataStore.env.POSTGRES_USER }}
            initialDelaySeconds: 30
            periodSeconds: 20
          readinessProbe:
            exec:
              command:
                - pg_isready
                - -U
                - {{ .Values.metadataStore.env.POSTGRES_USER }}
            initialDelaySeconds: 10
            periodSeconds: 10
          resources:
            {{- toYaml .Values.metadataStore.resources | nindent 12 }}
          volumeMounts:
            - name: postgres-data
              mountPath: /var/lib/postgresql/data
  {{- if .Values.metadataStore.persistence.enabled }}
  volumeClaimTemplates:
    - metadata:
        name: postgres-data
      spec:
        accessModes: ["ReadWriteOnce"]
        {{- if .Values.metadataStore.persistence.storageClass }}
        storageClassName: {{ .Values.metadataStore.persistence.storageClass }}
        {{- else if .Values.global.storageClass }}
        storageClassName: {{ .Values.global.storageClass }}
        {{- end }}
        resources:
          requests:
            storage: {{ .Values.metadataStore.persistence.size }}
  {{- end }}
{{- end }}
