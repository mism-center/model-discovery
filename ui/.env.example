# Used by SSR where relative URL isn't available
API_BASE_URL=http://localhost:8000

# Subdirectory under the resource version folder where annotation outputs are written.
# Defaults to "out" if unset.
VITE_ANNOTATION_OUTPUT_DIR=out
